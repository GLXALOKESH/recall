/**
 * Base URL for the backend API.
 * In a development environment, this should point to your Express server.
 */
export const BACKEND_URL = process.env.NEXT_PUBLIC_BACKEND_URL || 'https://api-proto.proveniq.co.in/' || 'http://localhost:5001';
